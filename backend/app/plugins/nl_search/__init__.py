"""``nl_search`` — natural-language search plugin.

Public-facing chat-style search over the platform's MCP-exposed
corpora. Off by default; activated from ``/admin/plugins``, configured
from Admin Settings (``nl_search_*`` keys), and surfaced on the
public home / header / footer via the ``public_navigation`` capability
(see [`docs/reference/PUBLIC_NAVIGATION.md`](../../../docs/reference/PUBLIC_NAVIGATION.md)
once it lands).
"""
