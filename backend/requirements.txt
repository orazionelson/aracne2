fastapi==0.125.0
starlette==0.49.1
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pydantic[email]==2.10.3
pydantic-settings==2.6.1
# PyJWT talks to ``cryptography`` directly — no transitive ``pyasn1``
# dependency, so the ASN.1-DoS exposure tracked in DEFERRED.md §15 is
# closed by virtue of that package no longer being installed at all.
# We migrated from ``python-jose`` (low-maintenance, last release
# 2024) on 2026-05-03; only ``app.services.auth`` consumed it.
PyJWT==2.10.1
cryptography==46.0.7
bcrypt==4.2.1
httpx==0.28.1
python-multipart==0.0.26
aiosmtplib==4.0.2
Jinja2==3.1.4
structlog==24.4.0
slowapi==0.1.9
defusedxml==0.7.1
lxml==6.1.0
pgvector==0.3.6
rdflib==7.1.1
apscheduler==3.10.4
markdown-it-py==3.0.0
bleach==6.2.0
prometheus-client==0.21.1
# pytest 9 + matching plugin majors landed on PyPI in 2026; the
# triple bump closes CVE-2025-71176 (DEFERRED.md §16) and unblocks
# test-suite usage of the new collection / async-fixture APIs.
pytest==9.0.3
pytest-asyncio==1.3.0
anyio==4.7.0
aiosqlite==0.20.0
ruff==0.8.4
mypy==1.13.0
coverage==7.13.5
pytest-cov==7.1.0
