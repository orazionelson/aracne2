"""Single source of truth for the CLI version string.

Imported by ``__init__.py`` and surfaced via ``aracne --version``. Keep
in lockstep with ``cli/pyproject.toml``'s ``version`` field.
"""

__version__ = "0.1.0"
